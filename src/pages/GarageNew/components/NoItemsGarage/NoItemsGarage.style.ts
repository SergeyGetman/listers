import { Box, styled } from '@mui/material';

export const NoItemsGarageContainer = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  gap: '16px',
  alignItems: 'center',
  justifyContent: 'center',
}));
