import { Box, styled } from '@mui/material';

export const ConfirmDeclineModalContainer = styled(Box)(() => ({
  height: '100%',
  width: '100%',
  padding: '16px',
  dispatch: 'flex',
  alignItems: 'flex-start',
  justifyContent: 'flex-start',
  flexDirection: 'column',
}));
