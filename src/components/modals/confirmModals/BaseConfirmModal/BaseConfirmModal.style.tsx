import { Box, styled } from '@mui/material';

export const BaseConfirmModalContainer = styled(Box)(() => ({
  height: '100%',
  width: '100%',
  dispatch: 'flex',
  alignItems: 'flex-start',
  justifyContent: 'flex-start',
  flexDirection: 'column',
}));
