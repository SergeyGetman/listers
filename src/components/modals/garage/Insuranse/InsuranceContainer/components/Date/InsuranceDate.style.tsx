import { styled } from '@mui/material';

export const InsuranceDateCheckboxContainer = styled('form')(() => ({
  display: 'flex',
  alignItems: 'flex-start',
  height: '100%',
  paddingTop: '30px',
}));
