import { Box, styled } from '@mui/material';

export const CreateContactModalContainerBtnContainer = styled(Box)(() => ({
  height: '36px',
  display: 'flex',
  alignItems: 'center',
}));

export const CreateContactModalContainerBtnContent = styled(Box)(() => ({
  width: '100%',
  display: 'flex',
  justifyContent: 'flex-end',
}));
