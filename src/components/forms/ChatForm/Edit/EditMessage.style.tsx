import { Box, styled } from '@mui/material';

export const EditMessageContainer = styled(Box)({
  margin: '10px 170px 0 20px',
  maxHeight: '46px',
  wordBreak: 'break-word',
  display: 'flex',
  overflow: 'hidden',
});
