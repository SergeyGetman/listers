import { Box, styled } from '@mui/material';

export const StubWithCreateVariantsContainer = styled(Box)(() => ({
  width: '100%',
  maxWidth: '714px',
  textAlign: 'center',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
}));

export const StubWithCreateVariantsItemsContainer = styled(Box)(() => ({
  marginTop: '24px',
  width: '100%',
  textAlign: 'center',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
}));
