export enum PlanItemsEnum {
  todo = 'todo',
  planner = 'planner',
  roadmap = 'roadmap',
  backlog = 'backlog',
  events = 'events',
  garage = 'garage',
  network = 'network',
  settings = 'settings',
  feedback = 'feedback',
  chat = 'chat',
  calendar = 'calendar',
  archive = 'archive',
}
