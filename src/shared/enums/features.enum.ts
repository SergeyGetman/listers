export enum FeaturesEnum {
  backlog = 'backlog',
  journal = 'journal',
  todo = 'todo',
  events = 'events',
  roadmap = 'roadmap',
  planner = 'planner',
  network = 'network',
  calendar = 'calendar',
  archive = 'archive',
  chat = 'chat',
  profile = 'chat',
}
