export enum ToolsEnum {
  deadlines = 'deadlines',
  meetings = 'meetings',
  appointment = 'appointment',
  events = 'events',
  reminders = 'reminders',
  tasks = 'tasks',
  checklists = 'checklists',
  payments = 'payments',
  notes = 'notes',
}
